<?php

function missingKey($key)
{   
    // if dont match any lang key this method log lang keys somewhere  only dev mode 
    // $line = time() . '|'.$key . '|' . $_SERVER['REQUEST_URI'] . "\n";
    // file_put_contents(LANG_FOLDER . LH::$lid . '_.bak', $line,FILE_APPEND);
}